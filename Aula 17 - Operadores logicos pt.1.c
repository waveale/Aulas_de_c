#include <stdio.h>
#include <stdlib.h>

//Tomar uma decisão significa, julgamento de uma situação que decidi o que o nosso programa deve fazer;
//Se a condição for estabelecida, execute essas instruções, se não, execute este outro bloco de instruções;
//Else --- "SE NAO"
int main()
{
    int i = 10;
    if (5==5);
    {
    
    }else{
    
    }

    return 0;
}